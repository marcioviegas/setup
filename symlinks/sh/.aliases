# vim:ft=sh

alias killvms="vagrant global-status | grep virtualbox | cut -c 1-9 | while read line; do echo $line; vagrant halt $line; done;"
alias recipe="bundle exec kitchen create"
alias cook="bundle exec kitchen converge"
alias burn="bundle exec kitchen destroy"
alias seatmap="cd Projects/LATAM/Ancilliary/LatamApiSeatMap"
alias home="cd ~"
alias m="make"
